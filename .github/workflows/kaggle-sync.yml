name: Sync to Kaggle

on:
  push:
    branches: [main]

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Kaggle CLI
        run: pip install kaggle

      - name: Configure Kaggle API Token (NEW)
        run: |
          mkdir -p ~/.kaggle
          echo "${{ secrets.KAGGLE_KEY }}" > ~/.kaggle/access_token
          chmod 600 ~/.kaggle/access_token
          rm -f ~/.kaggle/kaggle.json  # garante que não usa legacy

      - name: Test authentication
        run: |
          kaggle config view
          kaggle kernels list

      - name: Update Kaggle dataset
        run: |
          kaggle datasets version -p . -m "Auto-sync from commit ${{ github.sha }}"

      - name: Push notebooks to Kaggle
        run: |
          for dir in notebooks/*/; do
            echo "Enviando kernel em: $dir"
            kaggle kernels push -p "$dir"
          done
